<?php


// Cache server responses
$config['srcds_enable_cache'] = TRUE;

// Should we cache bad responses
$config['srcds_cache_bad_responses'] = TRUE;

// Time to cache responses for in seconds
$config['srcds_cache_time'] = 30;
